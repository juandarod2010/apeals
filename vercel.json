{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "comment": "Todo a index.html MENOS /api. La reescritura anterior era /(.*) y se tragaba las funciones: PayPal habria recibido el HTML de la portada con un 200, habria dado el webhook por entregado, y ningun pago se habria registrado. Sin un solo error.",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
